# frozen_string_literal: true

source "https://rubygems.org"

# The deployable dummy app uses a checked-in FlatPack snapshot so App Platform's
# automatic bundle install and rake detection do not depend on a mutable parent path.
gem "flat_pack", path: "vendor/flat_pack"

eval_gemfile "Gemfile.common"
