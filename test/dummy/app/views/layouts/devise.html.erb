<!DOCTYPE html>
<html class="h-full">
  <head>
    <title>FlatPack Demo &mdash; Sign In</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-full bg-[var(--surface-page-background-color)] text-[var(--surface-content-color)]" data-controller="dark-mode">
    <%= render "layouts/icon_sprite" %>

    <%= yield %>
  </body>
</html>
