<%= render FlatPack::PageTitle::Component.new(
  title: "Search Component",
  subtitle: "Drop a search field in the top bar, or anywhere else you need a quick find."
) %>
<%= render "pages/component_method_variables_table", component: :search %>

<section class="mb-12">
  <%= render FlatPack::SectionTitle::Component.new(title: "Local catalog", anchor_link: true, subtitle: "Filter a list in the browser. Titles beat descriptions, so form finds Forms first.") %>
  <p class="text-sm text-[var(--surface-muted-content-color)] mb-4">
    Pass <code>items:</code> when the list is already in the page. The dummy top nav uses this with <code>DemoCatalog.searchable_items</code>. Try typing <strong>form</strong>.
  </p>

  <div class="rounded-lg border border-[var(--surface-border-color)] p-6 bg-[var(--surface-background-color)] mb-4">
    <%= render FlatPack::Search::Component.new(
      items: DemoCatalog.searchable_items,
      placeholder: "Try form, accordion, or chart...",
      min_characters: 1,
      no_results_text: "No demo pages found"
    ) %>
  </div>

  <%= render FlatPack::CodeBlock::Component.new(
    code: CGI.unescapeHTML(<<~'CODE')
      &lt;%= render FlatPack::Search::Component.new(
        items: [
          {title: "Forms", description: "Inputs and submit patterns", url: "/demo/forms"},
          {title: "Tables: Basic", description: "Row formatting and filters", url: "/demo/tables/basic"}
        ],
        placeholder: "Search demo pages...",
        min_characters: 1
      ) %&gt;
    CODE
  ) %>
</section>

<section class="mb-12">
  <%= render FlatPack::SectionTitle::Component.new(title: "Remote JSON", anchor_link: true, subtitle: "Ask the server when results have to come from a query.") %>
  <p class="text-sm text-[var(--surface-muted-content-color)] mb-4">
    Use a <code>search_url</code> when the list is not static. Ignored if you also pass <code>items:</code>.
  </p>

  <div class="rounded-lg border border-[var(--surface-border-color)] p-6 bg-[var(--surface-background-color)] mb-4">
    <%= render FlatPack::Search::Component.new(
      search_url: demo_search_results_path,
      placeholder: "Search projects, files, or people..."
    ) %>
  </div>

  <%= render FlatPack::CodeBlock::Component.new(
    code: CGI.unescapeHTML(<<~'CODE')
      &lt;%= render FlatPack::Search::Component.new(
        search_url: demo_search_results_path,
        placeholder: "Search projects, files, or people..."
      ) %&gt;
    CODE
  ) %>
</section>

<section class="mb-12">
  <%= render FlatPack::SectionTitle::Component.new(title: "Theme Tokens", anchor_link: true, subtitle: "Dedicated --search-* tokens for the icon, field, and results.") %>
  <p class="text-sm text-[var(--surface-muted-content-color)] mb-4">
    Override these per theme or per container. The magnifying-glass alignment lives on the icon, not on this field.
  </p>

  <div class="rounded-lg border border-[var(--surface-border-color)] p-6 bg-[var(--surface-background-color)] mb-4">
    <div class="max-w-lg" style="--search-input-background-color: color-mix(in oklab, var(--color-primary) 8%, var(--surface-background-color)); --search-input-border-color: var(--color-primary); --search-input-focus-ring-color: var(--color-primary); --search-dropdown-border-color: var(--color-primary); --search-result-hover-background-color: color-mix(in oklab, var(--color-primary) 10%, var(--surface-background-color)); --search-icon-color: var(--color-primary);">
      <%= render FlatPack::Search::Component.new(
        items: DemoCatalog.searchable_items,
        placeholder: "Search with token overrides...",
        min_characters: 1,
        no_results_text: "No tokenized matches"
      ) %>
    </div>
  </div>

  <%= render FlatPack::CodeBlock::Component.new(
    language: "css",
    code: <<~CSS
      :root {
        --search-icon-color: var(--surface-muted-content-color);
        --search-input-background-color: var(--surface-muted-background-color);
        --search-input-border-color: transparent;
        --search-input-text-color: var(--surface-content-color);
        --search-input-placeholder-color: var(--surface-muted-content-color);
        --search-input-focus-ring-color: var(--color-primary);
        --search-dropdown-background-color: var(--surface-background-color);
        --search-dropdown-border-color: var(--surface-border-color);
        --search-dropdown-muted-text-color: var(--surface-muted-content-color);
        --search-result-title-color: var(--surface-content-color);
        --search-result-description-color: var(--surface-muted-content-color);
        --search-result-hover-background-color: var(--surface-muted-background-color);
        --search-result-divider-color: var(--surface-border-color);
      }
    CSS
  ) %>
</section>

<section class="mb-12">
  <%= render FlatPack::SectionTitle::Component.new(title: "Custom Name and Value", anchor_link: true, subtitle: "Start with a value, and name the field something other than q.") %>
  <div class="rounded-lg border border-[var(--surface-border-color)] p-6 bg-[var(--surface-background-color)] mb-4">
    <%= render FlatPack::Search::Component.new(
      name: "query",
      value: "flatpack",
      items: DemoCatalog.searchable_items,
      placeholder: "Search components...",
      min_characters: 1,
      no_results_text: "No matching components",
      class: "max-w-lg"
    ) %>
  </div>

  <%= render FlatPack::CodeBlock::Component.new(
    code: CGI.unescapeHTML(<<~'CODE')
      &lt;%= render FlatPack::Search::Component.new(
        name: "query",
        value: "flatpack",
        items: [{title: "Search", description: "Live search field", url: "/demo/search"}],
        placeholder: "Search components...",
        no_results_text: "No matching components",
        class: "max-w-lg"
      ) %&gt;
    CODE
  ) %>
</section>
