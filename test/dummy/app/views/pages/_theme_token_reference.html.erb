<section class="mt-12">
  <%= render FlatPack::SectionTitle::Component.new(
    title: "Theme Tokens",
    subtitle: "#{title} tokens from @theme (#{rows.size} total). Aliases that use var() inherit from semantic / brand tokens.",
    anchor_link: true
  ) %>

  <div class="mt-4 rounded-lg border border-[var(--surface-border-color)] bg-[var(--surface-background-color)] p-4">
    <%= render "themes/token_table", rows: rows %>
  </div>
</section>