<%= render FlatPack::PageTitle::Component.new(
  title: "Switch",
  subtitle: "Boolean toggle input examples with sizes and states."
) %>

<%= render "pages/component_method_variables_table", component: :text_input %>

<section class="mb-12">
  <%= render FlatPack::SectionTitle::Component.new(title: "Switch Examples", anchor_link: true) %>

  <%= render FlatPack::Card::Component.new do |card| %>
    <% card.body(class: "space-y-[var(--stack-gap-md)]") do %>
      <%= render FlatPack::Switch::Component.new(
        name: "feature_toggle",
        label: "Enable feature",
        help_text: "Turn this on to expose the feature to the current workspace."
      ) %>

      <%= render FlatPack::Switch::Component.new(
        name: "autosave_toggle",
        label: "Auto-save",
        help_text: "Auto-save keeps changes without a manual submit.",
        checked: true
      ) %>

      <%= render FlatPack::Switch::Component.new(
        name: "disabled_toggle",
        label: "Disabled toggle",
        help_text: "Disabled toggles show unavailable settings.",
        disabled: true
      ) %>
    <% end %>
  <% end %>

  <%= render FlatPack::CodeBlock::Component.new(
    code: CGI.unescapeHTML(<<~'CODE'),
      &lt;%= render FlatPack::Card::Component.new do |card| %&gt;
        &lt;% card.body do %&gt;
        &lt;%= render FlatPack::Switch::Component.new(
          name: &quot;feature_toggle&quot;,
          label: &quot;Enable feature&quot;,
          help_text: &quot;Turn this on to expose the feature to the current workspace.&quot;
        ) %&gt;
    
        &lt;%= render FlatPack::Switch::Component.new(
          name: &quot;autosave_toggle&quot;,
          label: &quot;Auto-save&quot;,
          help_text: &quot;Auto-save keeps changes without a manual submit.&quot;,
          checked: true
        ) %&gt;
    
        &lt;%= render FlatPack::Switch::Component.new(
          name: &quot;disabled_toggle&quot;,
          label: &quot;Disabled toggle&quot;,
          help_text: &quot;Disabled toggles show unavailable settings.&quot;,
          disabled: true
        ) %&gt;
        &lt;% end %&gt;
      &lt;% end %&gt;
    CODE
    language: "erb",
    title: "Switch Examples"
  ) %>
</section>
