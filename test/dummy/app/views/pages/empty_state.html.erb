<%= render FlatPack::PageTitle::Component.new(
  title: "Empty State Component",
  subtitle: "User-friendly empty states with optional icons and custom graphics."
) %>

<%= render "pages/component_method_variables_table", component: :empty_state %>

<!-- Basic Empty State -->
<section class="mb-12">
  <%= render FlatPack::SectionTitle::Component.new(title: "Basic Empty State", anchor_link: true, subtitle: "Examples for Basic Empty State.") %>
  <div class="border border-[var(--surface-border-color)] rounded-lg p-8">
    <%= render FlatPack::EmptyState::Component.new(
      title: "No messages",
      description: "You don't have any messages yet. Start a conversation to see it here."
    ) %>
  </div>
  
  <%= render FlatPack::CodeBlock::Component.new(
    code: CGI.unescapeHTML(<<~'CODE'),
      &lt;%= render FlatPack::EmptyState::Component.new(
        title: "No messages",
          description: "You don't have any messages yet."
      ) %&gt;
    CODE
    class: "mt-4"
  ) %>
</section>

<!-- Empty State with Actions -->
<section class="mb-12">
  <%= render FlatPack::SectionTitle::Component.new(title: "Empty State with Actions", anchor_link: true, subtitle: "Examples for Empty State with Actions.") %>
  <div class="border border-[var(--surface-border-color)] rounded-lg p-8">
    <%= render FlatPack::EmptyState::Component.new(
      title: "No projects found",
      description: "Get started by creating your first project. Projects help you organize your work."
    ) do |empty| %>
      <% empty.actions do %>
        <%= render FlatPack::Button::Component.new(text: "Create Project", style: :primary) %>
        <%= render FlatPack::Button::Component.new(text: "Import Project", style: :secondary) %>
      <% end %>
    <% end %>
  </div>
  
  <%= render FlatPack::CodeBlock::Component.new(
    code: CGI.unescapeHTML(<<~'CODE'),
      &lt;%= render FlatPack::EmptyState::Component.new(
        title: "No projects found",
        description: "Get started by creating your first project."
      ) do |empty| %&gt;
        &lt;% empty.actions do %&gt;
          &lt;%= render FlatPack::Button::Component.new(text: "Create Project", style: :primary) %&gt;
        &lt;% end %&gt;
      &lt;% end %&gt;
    CODE
    class: "mt-4"
  ) %>
</section>

<!-- Empty State with Icons -->
<section class="mb-12">
  <%= render FlatPack::SectionTitle::Component.new(title: "Icons", anchor_link: true, subtitle: "Examples for Icons.") %>
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <div class="border border-[var(--surface-border-color)] rounded-lg p-8">
      <%= render FlatPack::EmptyState::Component.new(
        title: "Inbox empty",
        description: "All caught up! No new messages.",
        icon: :inbox
      ) %>
    </div>
    
    <div class="border border-[var(--surface-border-color)] rounded-lg p-8">
      <%= render FlatPack::EmptyState::Component.new(
        title: "No results",
        description: "Try adjusting your search or filters.",
        icon: :search
      ) %>
    </div>

    <div class="border border-[var(--surface-border-color)] rounded-lg p-8">
      <%= render FlatPack::EmptyState::Component.new(
        title: "No files uploaded",
        description: "Upload files to get started.",
        icon: :upload
      ) %>
    </div>
  </div>
  
  <%= render FlatPack::CodeBlock::Component.new(
    code: CGI.unescapeHTML(<<~'CODE'),
      &lt;%= render FlatPack::EmptyState::Component.new(
        title: "Inbox empty",
        description: "All caught up!",
        icon: :inbox
      ) %&gt;
      
      &lt;%= render FlatPack::EmptyState::Component.new(
        title: "No results",
        description: "Try adjusting your search.",
        icon: :search
      ) %&gt;
      
      &lt;%= render FlatPack::EmptyState::Component.new(
        title: "No files uploaded",
        description: "Upload files to get started.",
        icon: :upload
      ) %&gt;
    CODE
    class: "mt-4"
  ) %>
</section>

<!-- Empty State in Tables -->
<section class="mb-12">
  <%= render FlatPack::SectionTitle::Component.new(title: "Empty State in Table", anchor_link: true, subtitle: "Examples for Empty State in Table.") %>
  <%= render FlatPack::Table::Component.new(data: []) do |table| %>
    <% table.column(title: "Name", html: ->(row) { row.name }) %>
    <% table.column(title: "Email", html: ->(row) { row.email }) %>
    <% table.column(title: "Status", html: ->(row) { row.status }) %>
  <% end %>
  
  <%= render FlatPack::CodeBlock::Component.new(
    code: CGI.unescapeHTML(<<~'CODE'),
      &lt;%= render FlatPack::Table::Component.new(data: []) do |table| %&gt;
        <!-- Table automatically shows empty state when data is empty -->
      &lt;% end %&gt;
    CODE
    class: "mt-4"
  ) %>
</section>

<!-- Empty State in Cards -->
<section class="mb-12">
  <%= render FlatPack::SectionTitle::Component.new(title: "Empty State in Card", anchor_link: true, subtitle: "Examples for Empty State in Card.") %>
  <%= render FlatPack::Card::Component.new(style: :outlined) do |card| %>
    <% card.header do %>
      <h3 class="text-lg font-semibold">Recent Activity</h3>
    <% end %>
    <% card.body do %>
      <%= render FlatPack::EmptyState::Component.new(
        title: "No activity yet",
        description: "Your recent activity will appear here.",
        icon: :inbox
      ) %>
    <% end %>
  <% end %>

  <%= render FlatPack::CodeBlock::Component.new(
    code: CGI.unescapeHTML(<<~'CODE'),
      &lt;%= render FlatPack::Card::Component.new(style: :outlined) do |card| %&gt;
        &lt;% card.header do %&gt;
          &lt;h3 class=&quot;text-lg font-semibold&quot;&gt;Recent Activity&lt;/h3&gt;
        &lt;% end %&gt;
        &lt;% card.body do %&gt;
          &lt;%= render FlatPack::EmptyState::Component.new(
            title: &quot;No activity yet&quot;,
            description: &quot;Your recent activity will appear here.&quot;,
            icon: :inbox
          ) %&gt;
        &lt;% end %&gt;
      &lt;% end %&gt;
    CODE
    language: "erb",
    title: "Empty State in Card"
  ) %>
</section>

<!-- Empty State with Custom Graphic -->
<section class="mb-12">
  <%= render FlatPack::SectionTitle::Component.new(title: "Empty State with Custom Graphic", anchor_link: true, subtitle: "Examples for Empty State with Custom Graphic.") %>
  <div class="border border-[var(--surface-border-color)] rounded-lg p-8">
    <%= render FlatPack::EmptyState::Component.new(
      title: "No data available",
      description: "Upload a CSV file or connect a data source to get started."
    ) do |empty| %>
      <% empty.graphic do %>
        <div class="w-32 h-32 rounded-full bg-gradient-to-br from-blue-100 to-purple-100 flex items-center justify-center mb-6">
          <%= render FlatPack::Shared::IconComponent.new(name: :upload, size: :lg, class: "text-blue-600") %>
        </div>
      <% end %>
      <% empty.actions do %>
        <%= render FlatPack::Button::Component.new(text: "Upload CSV", style: :primary) %>
        <%= render FlatPack::Button::Component.new(text: "Connect Source", style: :secondary) %>
      <% end %>
    <% end %>
  </div>

  <%= render FlatPack::CodeBlock::Component.new(
    code: CGI.unescapeHTML(<<~'CODE'),
      &lt;div class=&quot;border border-[var(--surface-border-color)] rounded-lg p-8&quot;&gt;
        &lt;%= render FlatPack::EmptyState::Component.new(
          title: &quot;No data available&quot;,
          description: &quot;Upload a CSV file or connect a data source to get started.&quot;
        ) do |empty| %&gt;
          &lt;% empty.graphic do %&gt;
            &lt;div class=&quot;w-32 h-32 rounded-full bg-gradient-to-br from-blue-100 to-purple-100 flex items-center justify-center mb-6&quot;&gt;
              &lt;%= render FlatPack::Shared::IconComponent.new(name: :upload, size: :lg, class: &quot;text-blue-600&quot;) %&gt;
            &lt;/div&gt;
          &lt;% end %&gt;
          &lt;% empty.actions do %&gt;
            &lt;%= render FlatPack::Button::Component.new(text: &quot;Upload CSV&quot;, style: :primary) %&gt;
            &lt;%= render FlatPack::Button::Component.new(text: &quot;Connect Source&quot;, style: :secondary) %&gt;
          &lt;% end %&gt;
        &lt;% end %&gt;
      &lt;/div&gt;
    CODE
    language: "erb",
    title: "Empty State with Custom Graphic"
  ) %>
</section>

<!-- Features -->
<section class="mb-12">
  <%= render FlatPack::SectionTitle::Component.new(title: "Features", anchor_link: true, subtitle: "Examples for Features.") %>
  <%= render FlatPack::List::Component.new(spacing: :dense, class: "text-[var(--surface-content-color)]") do %>
    <%= render FlatPack::List::Item.new(class: "py-0 px-0") do %><strong>Built-in Icons:</strong> Inbox and search icons included<% end %>
    <%= render FlatPack::List::Item.new(class: "py-0 px-0") do %><strong>Optional Icon:</strong> Pass <code>icon: :inbox</code>, <code>icon: :search</code>, or any icon name when needed<% end %>
    <%= render FlatPack::List::Item.new(class: "py-0 px-0") do %><strong>Custom Graphics:</strong> Slot for custom illustrations or icons<% end %>
    <%= render FlatPack::List::Item.new(class: "py-0 px-0") do %><strong>Action Buttons:</strong> Add primary and secondary CTAs<% end %>
    <%= render FlatPack::List::Item.new(class: "py-0 px-0") do %><strong>Centered Layout:</strong> Visually balanced and centered<% end %>
    <%= render FlatPack::List::Item.new(class: "py-0 px-0") do %><strong>Flexible Content:</strong> Title, description, and actions<% end %>
    <%= render FlatPack::List::Item.new(class: "py-0 px-0") do %><strong>Responsive:</strong> Adapts to container width<% end %>
  <% end %>
</section>
